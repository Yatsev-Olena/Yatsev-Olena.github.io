# Olena Yatsev — CV Website

Personal CV / portfolio site for **Olena Yatsev**, Data Engineer.
Built with plain HTML + CSS (no build step) and hosted on GitHub Pages.

**🌐 Live site:** https://yatsev-olena.github.io



The site auto-deploys on every push to `main`:

```bash
git add -A
git commit -m "Update CV site"
git push
```
